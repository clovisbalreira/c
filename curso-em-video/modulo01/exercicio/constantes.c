#include <stdio.h>
#define TOTAL 8

void main(){
    //const int TOTAL = 8;
    // TOTAL = 25;
    printf("%d", TOTAL);
}

/*
    Constantes
    #define é uma diretiva de pre-processamento;
    const é uma palvra reservada do c;
    com #define, o valor é substituido antes da compilação
    com o const, a constante existe na memória
    com const é possivel indicar o tipo primitivvo da constante
*/